window.GameBoxBangumi = {
    baseURL: 'https://api.bgm.tv',

    async search(name) {
        // Bangumi API adapter.
        // Search endpoint can be connected here.
        // Results should be normalized before entering the library.
        return [];
    }
};
