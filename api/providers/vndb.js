window.GameBoxVNDB = {
    token: '',

    async search(name) {
        // VNDB API adapter placeholder.
        // Token can be configured without exposing it in game data.
        return [];
    }
};
